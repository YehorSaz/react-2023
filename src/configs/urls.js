const baseURL = 'http://jsonplaceholder.typicode.com';

const carsURL = 'http://owu.linkpc.net/carsAPI/v1';


const urls = {
    user: '/users',
    comments: '/comments',
    cars: '/cars'
};

export  {
    baseURL,
    carsURL,
    urls
}
