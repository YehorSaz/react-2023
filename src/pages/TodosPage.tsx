import React, {FC} from 'react';
import {Todos} from "../components/todos/Todos";

const TodosPage: FC = () => {
    return (
        <div>
            <Todos/>
        </div>
    );
};

export {
    TodosPage
};